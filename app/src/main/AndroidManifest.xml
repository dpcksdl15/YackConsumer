<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yackconsumer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YackConsumer"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MdPriceResultActivity"
            android:exported="true" />
        <activity
            android:name=".MdPriceActivity"
            android:exported="true" />
        <activity
            android:name=".FirstMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MdSearchActivity" />
        <activity android:name=".MdSearchResultActivity" />
        <activity android:name=".ProvisionActivity" />
        <activity android:name=".Provision2Activity" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>